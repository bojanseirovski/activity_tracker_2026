services:
  web:
    build:
      context: .
      # "/" makes axios use the current origin; nginx proxies /api/* to the backend
      args:
        REACT_APP_API_URL: "/"
    ports:
      - "3001:80"

volumes:
  pgdata:
